name: todo_app_firestore_example
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  # firebase integration
  firebase_core: ^0.5.2+1
  cloud_firestore: ^0.14.3+1

  # slidable
  flutter_slidable: ^0.5.7

  # state management
  provider: ^4.3.2+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true